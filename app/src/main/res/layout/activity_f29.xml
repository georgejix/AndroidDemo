<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_marginTop="24dp"
    android:orientation="vertical">

    <TextView
        android:id="@+id/tv_download"
        style="@style/generalTv"
        android:text="download" />

    <TextView
        android:id="@+id/tv_filter_mp4"
        style="@style/generalTv"
        android:text="filter mp4" />

    <TextView
        android:id="@+id/tv_filter_mov"
        style="@style/generalTv"
        android:text="filter mov" />

    <TextView
        android:id="@+id/tv_filter_flv"
        style="@style/generalTv"
        android:text="filter flv" />

    <TextView
        android:id="@+id/tv_filter_avi"
        style="@style/generalTv"
        android:text="filter avi" />
</LinearLayout>
